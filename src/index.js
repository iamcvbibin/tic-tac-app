import React from "react";
import ReactDOM from "react-dom"; // Use this for older React versions (React 17 and below)
import App from "./App";
import "./App.css";

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById("root")
);
